import pandas as pd


def exclude(fm_doc_exports: pd.DataFrame, random_seed: int, printing_steps: bool = False) -> pd.DataFrame:
    # %% TODO NOW erstes exclude mithilfe von filename ohne Text selber...
    # TODO NOW zweites exclude anhand mr_translations Textstücken (OCR-Erkennung testen!)

    documents = pd.DataFrame()

    return documents
