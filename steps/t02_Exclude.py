import pandas as pd


def exclude(fm_doc_exports: pd.DataFrame, input_seed: int, printing_steps: bool = False) -> pd.DataFrame:
    # %% TODO NOW (III) erstes exclude mithilfe von filename ohne Text selber...
    # TODO NOW (III) zweites exclude anhand mr_translations Textstücken (OCR-Erkennung testen!)

    documents = pd.DataFrame()

    return documents
